import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    input: {
      backgroundColor: '#fff',
      paddingHorizontal: 10,
      paddingVertical: 8,
      borderRadius: 4,
      borderWidth: 0,
      borderColor: '#bdbdbd',
      fontSize: 16,
      color: 'blue',
      marginBottom: 10,
    },
  });