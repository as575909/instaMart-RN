import { StyleSheet } from "react-native";


export const styles = StyleSheet.create({
    wrapper: {
        display: "flex",
        flexDirection: "row",
        marginTop: 20,
        fontFamily: "Itim-Regular",
      },
      wrapperText: {
        marginLeft: 10,
        color: "#7d7d7d",
        fontFamily: "Itim-Regular",
      },
});